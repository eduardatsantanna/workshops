-- generated by cds-compiler version 2.5.2
COLUMN TABLE myapp_Sales (
  id INTEGER NOT NULL,
  cost INTEGER,
  vendor NVARCHAR(100),
  customer_id INTEGER,
  PRIMARY KEY(id)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN myapp_Customers AS customer ON (customer.id = customer_id)
)